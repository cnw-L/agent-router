# Testing Review

Look for regression tests on changed behavior, integration tests for shared contracts, UI/API workflow checks for user-facing changes, and meaningful assertions. Flag tests that are too weak, skipped without reason, or updated to match broken behavior.
