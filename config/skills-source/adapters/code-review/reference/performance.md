# Performance Review

Check algorithmic complexity, repeated IO/network calls, N+1 queries, unbounded memory growth, large payload handling, caching invalidation, and hot-path logging or serialization.
