# Python Review

Check exception handling, async/sync boundaries, resource cleanup, type hints when project uses them, mutable defaults, path handling, dependency boundaries, migrations, and test isolation.
