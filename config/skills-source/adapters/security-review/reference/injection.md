# Injection

Check SQL/NoSQL injection, command injection, template injection, path traversal, SSRF, XSS, unsafe eval, unsafe deserialization, and unvalidated redirects. Prefer parameterized APIs and allowlists.
