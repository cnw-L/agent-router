# Auth And Authorization

Check missing auth, confused roles, object-level authorization, tenant isolation, insecure defaults, session fixation, token expiry, CSRF where relevant, and privilege escalation through indirect object references.
